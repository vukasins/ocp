<?php

/**
 * Default configuration for router. This we can define default module, controller, action, etc.
 * @author Lajavi Krelac
 */
class Config_Request
{
	const DEFAULT_MODULE = 'page';
	const DEFAULT_ACTION = 'index'; // this should not be changeds
}